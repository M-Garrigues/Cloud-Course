name: Python application

on: [push]

jobs:
  build-app:
    runs-on: ubuntu-18.04
    steps:
    - uses: actions/checkout@v1
    - name: Set up Python 3.7
      uses: actions/setup-python@v1
      with:
        python-version: 3.7
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
    - name: Lint with flake8
      run: |
        pip install flake8
        # stop the build if there are Python syntax errors or undefined names
        flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics
        # exit-zero treats all errors as warnings. The GitHub editor is 127 chars wide
        flake8 . --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics

  python-test:
    needs: [build-app]
    runs-on: ubuntu-18.04
    steps:
      - name: Set up Python 3.7
        uses: actions/setup-python@v1
        with:
          python-version: 3.7
      - name: Test with pytest
        run: |
          pip install pytest
          pytest

  client-test:
    needs: [build-app]
    runs-on: ubuntu-18.04
    steps:
      - name: Setup Node
        uses: actions/setup-node@v1
        with:
          node-version: '10.x'
      - name: Checkout tools repo
        uses: actions/checkout@v2
        with:
          repository: Slashgear/cloud-client-eval
          path: client-test
      - name: Install client tests
        run: npm install
        working-directory: ./client-test
      - name: Run client tests - Part 1 (CRUD)
        run: npm run step:1
        working-directory: ./client-test
      - name: Run client tests - Part 2 (Pagination)
        run: npm run step:2
        working-directory: ./client-test
      - name: Run client tests - Part 3 (Search)
        run: npm run step:3
        working-directory: ./client-test
      - name: Run client tests - Part 3 Bis (Perf checks)
        run: npm run step:3bis
        working-directory: ./client-test
